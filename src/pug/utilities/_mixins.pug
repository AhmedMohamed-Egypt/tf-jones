- var ListCaseStudy = [{imgsrc:'img/case-1.png',title:'Company/ Development Name',subTtitle:'General Location in the UK',theme:'yellow-color'},{imgsrc:'img/case-2.png',title:'Company/ Development Name',subTtitle:'General Location in the UK',theme:'blue-color'}]
- var ListProjects=[{no:320,title:"Number of projects completed since 2010",theme:'th-white'},{no:56,title:"Apprentices taken on this year alone",theme:'th-yellow'},{no:4320,title:"Peple helped via community housing assiciations",theme:'th-min-green'}]
- var listCustomer = [{text:'“The bathroom is now my ‘show piece’ – I can now enjoy a shower without the fear of falling and I LOVE it! Thank you so much for using the ‘perfect’ contractors, they have worked their magic and I would highly recommend them!”',name:'Name Surname',company:'Company Name',date:'Date'},{text:'“The bathroom is now my ‘show piece’ – I can now enjoy a shower without the fear of falling and I LOVE it! Thank you so much for using the ‘perfect’ contractors, they have worked their magic and I would highly recommend them!”',name:'Name Surname',company:'Company Name',date:'Date'},{text:'“The bathroom is now my ‘show piece’ – I can now enjoy a shower without the fear of falling and I LOVE it! Thank you so much for using the ‘perfect’ contractors, they have worked their magic and I would highly recommend them!”',name:'Name Surname',company:'Company Name',date:'Date'}]

- var svgSpriteMono = 'img/sprite-mono.svg'
- var svgSpriteColor = 'img/sprite-color.svg'

mixin svgMono(name, modClass)
  if modClass == undefined
    - var modClass = ''
  else
    - var modClass = ' ' + modClass
  svg(class="ico ico-mono-" + name + modClass)
    use(xlink:href= svgSpriteMono + '#ico-mono-' + name)
      
mixin svgColor(name, modClass)
  if modClass == undefined
    - var modClass = ''
  else
    - var modClass = ' ' + modClass
  svg(class="ico ico-color-" + name + modClass)
    use(xlink:href= svgSpriteColor + '#ico-color-' + name)
mixin speak(className)
  if className == undefined 
    - var className = ''
  else 
    - var className  = ' ' + className
  section(class=`${className}`)
   .container
    div(id = 'carouselExampleDark' class=` carousel carousel-dark slide js speakSection d-flex flex-column `)
    .carousel-indicators.order-3
      button.active(type='button' data-bs-target='#carouselExampleDark' data-bs-slide-to='0' aria-current='true' aria-label='Slide 1')
      button(type='button' data-bs-target='#carouselExampleDark' data-bs-slide-to='1' aria-label='Slide 2')
      button(type='button' data-bs-target='#carouselExampleDark' data-bs-slide-to='2' aria-label='Slide 3')
    .carousel-inner
      .carousel-item.active(data-bs-interval='10000')
        // <img src="..." class="d-block w-100" alt="...">
        // Instead of image, div with style and min-height
        div()
          .carousel-caption.d-block.col-xl-7.mx-auto.col-12
            h5(class="text-blue-bold-28") “Can’t speak too highly of the team who did the work – very professional and pleasant.”
            p(class="text-blue-semiBold-28") Stockport Homes Group Residents
            
        // end of div style min-height: 180px;
      .carousel-item(data-bs-interval='2000')
        // Instead of image, div with style and min-height
        div()
          .carousel-caption.d-block.col-xl-7.mx-auto.col-12
            h5(class="text-blue-bold-28") “Can’t speak too highly of the team who did the work – very professional and pleasant.” 
            p(class="text-blue-semiBold-28") Stockport Homes Group Residents
        // end of div style min-height: 180px;
      .carousel-item
        // Instead of image, div with style and min-height
        div()
          .carousel-caption.d-block.col-xl-7.mx-auto.col-12
            h5(class="text-blue-bold-28") “Can’t speak too highly of the team who did the work – very professional and pleasant.”
            p(class="text-blue-semiBold-28") Stockport Homes Group Residents
mixin caseStudy(className)
 section(class=`case-study ${className}`)
 .container 
  div(class="case-study__container")
   .row.flex-column.flex-lg-row.col-xl-10.mx-auto.col
    .mb-3.mb-xl-0.col-xl-4.col
     h3(class="text-blue-48-bold lh-s1 case-study__mainTitle") Case Studies 
     p(class="text-navy-16") Check out our case studies on the services that we provide. 
    .col-xl-8.col
     .row.flex-column.flex-lg-row
      each val,index in ListCaseStudy 
       .col(class=`${index===0?'mb-3 mb-lg-0':''}`)
        a(href="#" class=`d-block ${val.theme}`)
         img(src=`${val.imgsrc}` class="img-fluid w-100 ")
         h3(class="text-navy-16 f-700 mb-0 mt-1")=val.title 
         h2(class="text-navy-16 f-400")=val.subTtitle
mixin hero(heading,text,className)
 div(class=`shrink-container d-flex align-items-center justify-content-between py-4 mb-5 ${className}`)
  div(class="hero-serivces__container--title position-relative")
   h2(class="h2-heading position-relative serviceWord")=heading
  span(class="hero-serivces__verticalLine d-block mx-3")
  p(class="tfBluecolor  size-22 f-500 lh-s3 hero-serivces__rightText")=text
mixin noProjects(list,className)
 section(class=`no-of-projects ${className}`)
  .container
   div(class="bg-tfBlue d-flex no-of-projects__container justify-content-evenly py-5")
    each val in list 
     div(class=`${val.theme}`)
      h3(class="size-60 f-900 mb-0")=val.no 
      p(class="f-600 size-20")=val.title
mixin carousel()
 #carouselExampleDark.carousel.carousel-dark.slide.d-flex.flex-column
    .carousel-indicators.order-3.w-100.position-static.m-0
      button.active(type='button' data-bs-target='#carouselExampleDark' data-bs-slide-to='0' aria-current='true' aria-label='Slide 1')
      button(type='button' data-bs-target='#carouselExampleDark' data-bs-slide-to='1' aria-label='Slide 2')
      button(type='button' data-bs-target='#carouselExampleDark' data-bs-slide-to='2' aria-label='Slide 3')
    .carousel-inner
     each val,index in listCustomer 
      .carousel-item(class=`${index==0?'active':''} `)
       div()
        .carousel-caption.d-block.w-100.position-static
         p(class="color-tfNavy f-400 size-20 mb-1")=val.text 
         h4(class="color-tfNavy f-500 size-20")=val.name 
         h5(class="color-tfNavy f-400 size-20")=val.company
         img(src="img/qoute.png" class="customer-saying__qoute")
mixin customerSay(className)
 section(class=`customer-saying bg-tfMint ${className}`)
  .container 
   div(class="customer-saying__container shrink-container  py-5")
    .row.py-4
     .col-4
      h2(class="h2-heading lh-1") What Our Customer are Saying
     .col-8
       div(class="customer-saying__carousel")
        +carousel

mixin contactExpert(className)
 section(class=`bg-navy mb-5 team-contact mt-5 ${className}`)
    .container.py-5 
        .row.justify-content-between.align-items-center.shrink-container
            .col-12.col-md-8.p-0
                h3(class="mb-md-1 mb-4") Contact our expert team for more information 
            .col-12.col-md-4.p-0
                a(class="contact-button" href="#") Get in Touch

 